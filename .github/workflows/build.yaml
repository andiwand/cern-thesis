name: Build

on:
    push:
    pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: hspaans/latexmk-action@v2.0.0
        with:
          format: pdf
          filename: thesis/thesis.tex
          options: -shell-escape
